/* eslint-env mocha */
/* eslint-disable */

import { assert } from 'chai';


describe.only('Simple test case', function() {
  it('asserts that 2 + 2 is 4', function() {
    assert.equal(2 + 2, 4);
  })
});
